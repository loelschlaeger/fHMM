# List to vector

This function produces a `vector` from a `list` structure and replaces
`NULL` elements by `NA`.

## Usage

``` r
list_to_vector(x)
```

## Arguments

- x:

  \[[`list()`](https://rdrr.io/r/base/list.html)\]  
  A `list`.

## Value

A `numeric`.
